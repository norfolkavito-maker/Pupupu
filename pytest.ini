[pytest]
# The application contains modules like `strategy_test.py` with public helpers
# named `test_*`. Without this config, running plain `pytest` would try to
# collect them as tests.
testpaths = tests
python_files = test_*.py *_unittest.py
addopts = -ra
